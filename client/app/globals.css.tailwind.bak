@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --brand: #2563eb; /* blue-600 */
  }

  body {
    @apply bg-gray-50 text-gray-900 antialiased;
    font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    line-height: 1.4;
  }
}

@layer components {
  /* small helper badges */
  .badge-open {
    @apply inline-block text-xs font-medium px-2 py-0.5 rounded-full bg-green-100 text-green-800;
  }
  .badge-closed {
    @apply inline-block text-xs font-medium px-2 py-0.5 rounded-full bg-gray-100 text-gray-800;
  }

  /* small container style for admin blocks */
  .admin-card {
    @apply bg-white rounded-2xl shadow-sm p-4;
  }
}
